<?xml version="1.0" encoding="UTF-8"?>

<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:gedmo="http://gediminasm.org/schemas/orm/doctrine-extensions-mapping">
    <mapped-superclass name="Owl\Component\Invoice\Model\Invoice" table="owl_invoice">
        <id name="id" column="id" type="integer">
            <generator strategy="AUTO" />
        </id>

        <field name="sequenceNumber" column="sequence_number" type="integer" nullable="true" />
        <field name="fullNumber" column="full_number" />
        <field name="type" column="type" />
        <field name="issueDate" column="issue_date" type="date" />
        <field name="transactionDate" column="transaction_date" type="date" />
        <field name="duePaymentDate" column="due_payment_date" type="date" />
        <field name="paymentDate" column="payment_date" type="date" nullable="true" />
        <field name="paymentMethod" column="payment_method" />
        <field name="isPaid" column="is_paid" type="boolean" />
        <field name="subtotal" type="integer" />
        <field name="taxTotal" column="tax_total" type="integer" />
        <field name="total" type="integer" />
        <field name="calculateValuesFrom" column="calculate_values_from" />

        <many-to-one field="buyer" target-entity="Owl\Component\Invoice\Model\Buyer\BuyerInterface" orphan-removal="true">
            <join-column name="buyer_id" referenced-column-name="id" nullable="false" />
            <cascade>
                <cascade-all/>
            </cascade>
        </many-to-one>

        <many-to-one field="serie" target-entity="Owl\Component\Invoice\Model\InvoiceSerieInterface">
            <join-column name="serie_id" referenced-column-name="id" nullable="true" on-delete="SET NULL" />
        </many-to-one>

        <one-to-many field="lineItems" target-entity="Owl\Component\Invoice\Model\LineItemInterface" mapped-by="invoice" orphan-removal="true">
            <cascade>
                <cascade-all/>
            </cascade>
        </one-to-many>

        <field name="createdAt" column="created_at" type="datetime">
            <gedmo:timestampable on="create"/>
        </field>
        <field name="updatedAt" column="updated_at" type="datetime" nullable="true">
            <gedmo:timestampable on="update"/>
        </field>

    </mapped-superclass>
</doctrine-mapping>
