{% for file in data %}
    <a class="badge bg-blue text-blue-fg text-decoration-none mb-1" href="{{ file.path|owl_file_path }}" target="_blank">{{ file.name }}</a>
    <div class="clearfix"></div>
{% endfor %}
