{% set form = hookable_metadata.context.form %}
{% set configuration = hookable_metadata.configuration %}

{% set defaultMessageError = configuration.default_message_error|default('owl.ui.error_while_adding_permissions')|trans %}

<div>
    {{ 
        form_start(form, {
            'name' : hookable_metadata.context.name~'-'~form.vars.value.name ,
            attr: {
                'data-controller': 'permission',
                'data-action': 'permission:changed@window->toast#show',
                'data-permission-assign-url-value': path(configuration.path.assign.route),
                'data-permission-revoke-url-value': path(configuration.path.revoke.route, { name: form.vars.value.name }),
                'data-permission-default-message-error-value': defaultMessageError,
            }
        }) }}
    <div class="form-check form-switch">
        <input {{form.vars.checked}} type="checkbox" class="form-check-input" data-permission-target="checkbox">
        <label class="checkbox-switch form-check-label">{{form.vars.description_permission|trans}}</label>
    </div>
    {{ form_row(form.name, { 'id' : form.vars.value.name~'-name'}) }}
    {{ form_row(form.group_permission, { 'id' : form.vars.value.name~'-type'}) }}
    {{ form_row(form.description, { 'id' : form.vars.value.name~'-type'}) }}
    {{ form_row(form._csrf_token, { 'id' : form.vars.value.name~'-_csrf_token'}) }}
    {{ form_end(form, {'render_rest': false}) }}
</div>