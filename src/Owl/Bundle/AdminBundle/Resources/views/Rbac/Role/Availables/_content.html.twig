{% 
    set add_url = path(
        'owl_admin_ajax_rbac_role_assign',
        { id: role.id }
    )
%}
{%
    set remove_url = path(
        'owl_admin_ajax_rbac_role_revoke',
        { id: role.id }
    )
%}

<div 
    id="list-permissions" 
    data-add-url="{{add_url}}" 
    data-add-with-form-data="true" 
    data-remove-url="{{remove_url}}" 
    data-remove-with-form-data="true" 
    class="ui stackable grid"
>
    {% for name, form in forms %}
        {% include '@OwlAdmin/Rbac/Role/Availables/Content/_item_accordion.html.twig' %}
    {% endfor %}
</div>
